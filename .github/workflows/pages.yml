name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:
  workflow_run:
    workflows:
      - Monitor climate events
      - Publish Climate Pulse reports
      - Prepare FAO CROPGRIDS 2020 reference
      - Prepare CRU annual climate context
      - Prepare CRU monthly seasonal context
      - Prepare NASA MODIS 2024 landcover reference
      - Prepare population reference
    types: [completed]
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout latest main
        uses: actions/checkout@v4
        with:
          ref: main
      - name: Checkout isolated production data
        uses: actions/checkout@v4
        with:
          ref: production-data
          path: .production-data
          fetch-depth: 1
      - name: Overlay latest isolated production data
        run: python scripts/production_data_branch.py restore --store .production-data --paths data/events data/exposure/population data/exposure/assets data/footprints data/climate/event_timeseries data/history/daily data/reports reports data/reference/crops/cropgrids_2020 data/reference/climate/cru_ts_4.10 data/reference/landcover/modis_mcd12c1_2024 data/reference/population
      - name: Validate public science copy
        run: python scripts/check_public_science_copy.py
      - name: Build filtered Pages artifact
        run: python scripts/build_public_dist.py --check
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: public-dist
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
