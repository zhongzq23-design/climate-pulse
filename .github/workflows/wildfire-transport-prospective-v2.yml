name: Wildfire transport prospective v2

on:
  workflow_dispatch:
  schedule:
    - cron: '25 13 * * *'

permissions:
  contents: write

concurrency:
  group: wildfire-transport-prospective-v2
  cancel-in-progress: false

jobs:
  freeze:
    runs-on: ubuntu-latest
    environment: production-secrets
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: main
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check earthengine-api shapely pyproj numpy rasterio
      - name: Freeze current wildfire predictions before outcomes
        env:
          GEE_PROJECT_ID: ${{ vars.EE_PROJECT || secrets.EE_PROJECT || secrets.GEE_PROJECT_ID }}
          GEE_SERVICE_ACCOUNT_JSON: ${{ secrets.EE_SERVICE_ACCOUNT_JSON || secrets.GEE_SERVICE_ACCOUNT_JSON }}
        run: |
          stamp="$(date -u +'%Y%m%dT%H%M%SZ')"
          out="data/research/wildfire_transport_prospective_v2/frozen/$stamp.json"
          python scripts/wildfire_transport_prospective_v2.py freeze --output "$out"
          python scripts/wildfire_transport_prospective_v2.py verify --input "$out"
          echo "FROZEN_OUTPUT=$out" >> "$GITHUB_ENV"
          cp "$out" /tmp/prospective-freeze.json
      - name: Commit immutable prediction freeze
        shell: bash
        run: |
          set -euo pipefail
          git add "$FROZEN_OUTPUT"
          if git diff --cached --quiet; then exit 0; fi
          git config user.name 'climate-pulse-bot'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git fetch origin research-data
          git switch -C research-data origin/research-data
          mkdir -p "$(dirname "$FROZEN_OUTPUT")"
          cp /tmp/prospective-freeze.json "$FROZEN_OUTPUT"
          git add "$FROZEN_OUTPUT"
          git commit -m "research: freeze prospective wildfire transport predictions"
          git push origin research-data
