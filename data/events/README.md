# Event data

`latest.json` is the stable browser-readable entry point for the current normalized/approved event snapshot. Historical snapshots can be stored under `archive/YYYY/MM/DD.json`.

Do not treat ingestion as publication approval or climate attribution.
