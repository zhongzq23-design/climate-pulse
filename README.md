# Climate Pulse production data

This branch is intentionally data-only. Trusted executable workflows and scripts live on the protected `main` branch. Automated production jobs may update only `data/` and `reports/` here.
